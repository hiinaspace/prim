# Linux OpenXR runtime compatibility

Fix VR startup with newer Monado/Envision builds that require GLIBC_2.43.
The portable Linux game now includes a matched glibc 2.43 runtime.

This Linux-only packaging update retains the 0.3.6 game, engine, and room
protocol. Windows stays at 0.3.6 and remains compatible in the same room.
